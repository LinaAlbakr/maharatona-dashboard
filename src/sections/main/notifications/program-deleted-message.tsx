/** @deprecated Prefer `program-notification-message`. Re-exports for existing imports. */
export {
  stripDecorators,
  parseProgramDeletedMessage,
  renderProgramDeletedMessage,
  parseProgramCreatedMessage,
  renderProgramCreatedMessage,
  renderProgramLifecycleMessage,
  renderProgramNameTitle,
} from './program-notification-message';
